# WeiXinAI - AI 角色扮演聊天 APP 依赖包
# 版本: 2.0.0
# 目标: 纯本地 Android APP (Chaquopy), 最小化依赖, APK < 80MB

# ============================================================
# 核心依赖 (必须)
# ============================================================

# HTTP 请求 (DeepSeek API 调用)
# 使用 requests 而非 httpx — requests 更小且 Chaquopy 内置支持更好
requests>=2.31.0

# 环境变量管理
python-dotenv>=1.0.0

# 日志 (更简洁的 API, 比标准 logging 更好用)
loguru>=0.7.0

# 数据验证 (Pydantic 已被 Chaquopy 内置部分支持, 可选)
# pydantic>=2.0.0  # 注释掉: Android 上体积较大, 用 dataclass 替代

# ============================================================
# Web 服务依赖 (仅桌面调试模式使用, Android 上不需要)
# ============================================================
# fastapi>=0.100.0   # 注释掉: Android 上不需要 Web 服务
# uvicorn>=0.20.0    # 注释掉: Android 上不需要 Web 服务

# ============================================================
# 可选依赖 (按需安装)
# ============================================================

# 中文分词 (后续可接入, 提升关键词提取质量)
# jieba>=0.42.1      # 注释掉: 初期用 bigram 替代

# 加密导出 (用户需要加密导出时安装)
# cryptography>=41.0.0  # 注释掉: 初期不引入

# ============================================================
# 语音和图片识别 (暂缓)
# ============================================================
# pillow>=10.0.0
# pytesseract>=0.3.10
# gtts>=2.3.0
# edge-tts>=6.0.0

# ============================================================
# 本地模型支持 (可选)
# ============================================================
# llama-cpp-python>=0.2.0
# transformers>=4.30.0
# torch>=2.0.0

# ============================================================
# 开发依赖 (桌面端开发时安装)
# ============================================================
# fastapi>=0.100.0    # 桌面端 API 服务
# uvicorn>=0.20.0     # 桌面端 API 服务
# pytest>=7.4.0       # 测试框架
# pytest-asyncio>=0.21.0
# black>=23.7.0       # 代码格式化
# isort>=5.12.0       # 导入排序

# ============================================================
# 总结: 当前最小依赖 = requests + python-dotenv + loguru
# 预计 APK 体积贡献: ~5MB
# ============================================================