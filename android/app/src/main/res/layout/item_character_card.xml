<?xml version="1.0" encoding="utf-8"?>
<!-- 角色卡片列表项：头像 + 名称简介 + 当前指示 + 编辑/删除 -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingHorizontal="16dp"
    android:paddingVertical="12dp"
    android:background="?attr/selectableItemBackground">

    <!-- 左侧：圆形头像边框占位 -->
    <ImageView
        android:id="@+id/ivCharacterAvatar"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:background="@drawable/bg_avatar_border"
        android:scaleType="centerCrop"
        android:contentDescription="角色头像" />

    <!-- 中间：名称 + 简介预览 -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical"
        android:layout_marginStart="12dp"
        android:layout_marginEnd="8dp">

        <TextView
            android:id="@+id/tvCharacterName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="@color/text_primary" />

        <TextView
            android:id="@+id/tvCharacterPreview"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="12sp"
            android:textColor="@color/text_secondary"
            android:maxLines="1"
            android:ellipsize="end"
            android:layout_marginTop="2dp" />
    </LinearLayout>

    <!-- 右侧：当前指示绿点 + 编辑 + 删除 -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <!-- 当前选中指示绿点 -->
        <View
            android:id="@+id/ivCurrentDot"
            android:layout_width="8dp"
            android:layout_height="8dp"
            android:background="@drawable/bg_status_online"
            android:visibility="gone"
            android:layout_marginEnd="8dp" />

        <!-- 编辑按钮 -->
        <TextView
            android:id="@+id/btnEditCharacter"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:text="✎"
            android:textSize="18sp"
            android:textColor="@color/text_secondary"
            android:gravity="center"
            android:clickable="true"
            android:focusable="true"
            android:contentDescription="编辑角色"
            android:layout_marginEnd="4dp" />

        <!-- 删除按钮 -->
        <TextView
            android:id="@+id/btnDeleteCharacter"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:text="✕"
            android:textSize="16sp"
            android:textColor="@color/accent_red"
            android:gravity="center"
            android:clickable="true"
            android:focusable="true"
            android:contentDescription="删除角色" />
    </LinearLayout>
</LinearLayout>