# AI 角色扮演聊天 APP — 项目完整报告

> 版本：v1.0-final | 日期：2026-06-04
> 状态：**已完成讨论 + 三智能体交叉审核，等待用户最终决策**
> 审核：架构师 + QA 测试 + 后端开发

---

## 目录

1. [项目概述](#1-项目概述)
2. [需求确认](#2-需求确认)
3. [技术方案](#3-技术方案)
4. [架构设计](#4-架构设计)
5. [模块详细设计](#5-模块详细设计)
6. [开发路线图](#6-开发路线图)
7. [API 成本分析](#7-api-成本分析)
8. [风险评估与缓解](#8-风险评估与缓解)
9. [交叉审核意见汇总](#9-交叉审核意见汇总)
10. [待决策事项](#10-待决策事项)
11. [附录](#11-附录)

---

## 1. 项目概述

### 1.1 项目定位

一个**纯本地运行的 Android AI 角色扮演聊天 APP**，单 APK 安装即用。核心目标是让 AI 角色像真人一样与用户聊天，具备记忆、主动发起话题等能力。

### 1.2 核心约束

| 约束 | 说明 |
|------|------|
| 无服务器 | 不能依赖任何服务器，纯本地运行 |
| 单 APK | 一个安装包，安装即用 |
| 低成本 | 月 API 成本控制在 ¥5 以内 |
| 好架构 | 可维护、可扩展，不能是屎山 |

### 1.3 分工

- **AI（Trae）**：负责全部代码编写
- **用户**：负责需求定义和测试验收

---

## 2. 需求确认

### 2.1 三大核心体验

#### 聊天质量 — "像真人在微信上聊天"

- 多段短对话为主（模拟微信聊天风格）
- 偶有长对话
- 角色卡驱动，人设稳定不崩塌
- 对话风格自然，避免机械感

#### 记忆系统 — "像真人的记忆"

三层记忆架构：
- 短期记忆：最近 50 轮对话，保证对话连贯性
- 长期记忆：普通记忆（规则+LLM提取，时间衰减）+ 强化记忆（用户/AI标记，几乎不衰减，检索权重×2）
- 关联记忆：记忆之间的关联关系（初期简化）

#### 主动消息 — "AI 能主动找你聊天"

- 用户设定起床时间和睡觉时间
- 起床时间 → 第一次主动消息，睡觉时间 → 最后一次主动消息
- 休息时段默认不发，除非角色卡强烈想发
- 活跃时段按用户设定的间隔随机发送

### 2.2 角色卡 + 世界书

**角色卡**：JSON 格式，定义角色性格、背景、对话风格、记忆关注点等。

**世界书**：JSON 格式，定义世界观。默认"三次元现实世界"，用户可自行添加。

### 2.3 语音功能

- 不需要每句话都转语音
- AI 角色卡自行决定是否发送语音
- TTS 文字转语音功能待定，暂不实现

---

## 3. 技术方案

### 3.1 方案选择：Chaquopy

**Chaquopy** = Android 原生 APP 内嵌 Python 引擎。

选择理由：
- Python 写 AI 逻辑，代码可复用 v1 的 chat_engine 和 api_client
- Kotlin Compose 写 UI，原生体验
- 单 APK 安装，不需要 Termux 或服务器
- Chaquopy 免费开源（MIT 许可证）

技术事实：
- Chaquopy 在 APK 中嵌入 Python 3.10/3.11 运行时
- 纯 Python 包（如 `requests`）完全兼容
- 含 C 扩展的包（如 `chromadb`、`sentence-transformers`）**不可用**
- APK 体积增加约 40-50MB

### 3.2 Python 包兼容性清单

| 包名 | Chaquopy 兼容性 | 说明 |
|------|----------------|------|
| `requests` | **完全兼容** | 纯 Python，v1 核心依赖 |
| `sqlite3` | **内置支持** | Python 标准库 |
| `json` | **内置支持** | Python 标准库 |
| `python-dotenv` | **完全兼容** | 纯 Python |
| `numpy` | **Chaquopy 预编译支持** | 有 Android wheel，约 4.8MB |
| `scikit-learn` | **Chaquopy 预编译支持** | 有 Android wheel，约 8.4MB |
| `chromadb` | **不可用** | 依赖 onnxruntime、hnswlib 等 C 扩展 |
| `sentence-transformers` | **不可用** | 依赖 PyTorch，太重 |
| `faiss-cpu` | **不可用** | 无 Android wheel |

### 3.3 向量检索方案：DeepSeek Embedding API + 纯 Python 余弦相似度

由于 ChromaDB 不可用，采用以下方案：
- 存储：SQLite（JSON 字段存储向量）
- 嵌入：DeepSeek Embedding API 生成向量
- 检索：纯 Python 余弦相似度计算

- 5000 条记忆以内，检索耗时约 50-100ms
- 零额外依赖，不增加 APK 体积
- 如果未来记忆量超过 10000 条，可引入 numpy 加速

---

## 4. 架构设计

### 4.1 模块依赖图

```
Application (Android Kotlin)
    │
    ├── RolePlayer（对话入口）
    │   ├── ContextManager（短期记忆）
    │   ├── CardManager（角色卡）
    │   ├── WorldBookManager（世界书）
    │   ├── MemoryStorage（长期记忆检索）
    │   │   ├── VectorStore（SQLite + 向量）
    │   │   └── MemoryExtractor（规则 + LLM）
    │   ├── PromptBuilder（组装 prompt）
    │   └── DeepSeekClient（API 调用）
    │
    └── ProactiveWorker（WorkManager 触发）
        └── ProactiveEngine（Python 决策）
            ├── TopicGenerator（话题生成）
            └── DeepSeekClient（API 调用）
```

### 4.2 v1 旧代码复用分析

| 模块 | 复用率 | 方式 |
|------|--------|------|
| `chat_engine/card_parser.py` | 95% | 直接复用，改文件路径 |
| `chat_engine/card_manager.py` | 90% | 直接复用 |
| `chat_engine/context_manager.py` | 70% | 重构，增加世界书/记忆注入 |
| `chat_engine/role_player.py` | 40% | 拆分为 RolePlayer + PromptBuilder |
| `api_client/base.py` | 100% | 直接复用 |
| `api_client/deepseek.py` | 90% | 直接复用，改模型名和超时 |
| `api_client/web_search.py` | 85% | 直接复用 |
| `memory/storage.py` | 30% | SQLite 基础可复用，其余重写 |
| `config/settings.py` | 70% | 精简后复用 |
| `wx_adapter/` | 0% | 完全废弃 |
| `api_server.py` | 0% | 完全废弃 |
| `templates/` | 0% | 完全废弃 |

---

## 5. 模块详细设计

### 5.1 聊天引擎

数据流：
```
用户输入
  → ContextManager 构建上下文
    ├─ 短期记忆：最近 50 轮对话
    ├─ 长期记忆：向量检索 Top-5
    └─ 世界书：当前世界设定
  → PromptBuilder 组装 System Prompt
    ├─ 角色卡设定
    ├─ 对话风格指引
    ├─ 世界书内容
    └─ 记忆检索结果
  → DeepSeekClient 调用 API
  → 返回回复 → 保存到短期记忆
  → 异步触发记忆提取
```

### 5.2 记忆系统

**短期记忆**：最近 50 轮对话，`deque` 数据结构，每轮对话后自动更新，直接拼入上下文。

**长期记忆**：
- 普通记忆：规则提取（免费）+ LLM 提取（付费，重要对话才触发）
- 时间衰减：`score = importance × e^(-λ × days)`，λ=0.01
- 强化记忆（Pinned）：用户说"记住"、"别忘了"、点按钮，或角色卡认定重要
- 衰减系数 λ=0.001（极慢），检索时权重 ×2.0

**关联记忆**：初期简化，同一对话 session 内的记忆自动关联，后续升级基于关键词 + 向量相似度。

### 5.3 主动消息引擎

决策逻辑：间隔检查 → 时间窗口检查 → 随机因子（±30%）→ 起床/睡觉特殊消息

与 Android WorkManager 配合：WorkManager 每30分钟触发 → ProactiveWorker → 调用 Python 引擎 → 弹出本地通知

### 5.4 世界书

默认世界书：三次元现实世界，无魔法、无超能力、无科幻元素。
注入方式：按需检索而非全量注入，避免浪费 token。

---

## 6. 开发路线图

### 优化后的路线（基于架构师审核建议）

| 阶段 | 内容 | 目标 | 关键验证点 |
|------|------|------|-----------|
| **P0: Python 原型** | 核心对话 + 记忆 + 主动消息，命令行运行 | 验证 AI 逻辑正确 | DeepSeek API 连通、角色扮演质量、记忆提取准确率 |
| **P1: 架构 + Chaquopy 验证** | 项目骨架 + 验证 Chaquopy 核心依赖 | 确认技术方案可行 | `requests`/`sqlite3` 在 Chaquopy 中正常 |
| **P2: 核心对话** | 角色卡引擎、上下文管理、API 调用 | 能聊天 | 10 轮对话人设不崩塌 |
| **P3: 记忆系统** | 短期 + 长期记忆（混合提取、向量检索） | 有记忆 | 记忆提取准确率 > 80% |
| **P4: Android 封装** | Chaquopy 集成 + Kotlin UI + APK 打包 | 手机上能用 | APK 安装运行，内存不泄露 |
| **P5: 主动消息** | 定时引擎 + WorkManager + 通知 | 会主动 | 休息时段 0 误触发 |
| **P6: 世界书** | 加载、管理、按需注入 | 世界设定 | 与角色卡联动正常 |
| **P7: 打磨** | 测试、优化、关联记忆 | 可发布 | 24h 稳定性测试 |

### 路线调整说明

相比初版方案的关键调整：
1. **P1 加入 Chaquopy 可行性验证**：在架构搭建同时验证核心依赖，避免后期翻车
2. **P4 Android 封装提前**：从 Python 命令行到 APK 的跨越是最大技术风险，尽早验证
3. **P5 主动消息后移**：主动消息依赖 WorkManager（Kotlin 侧），Android 封装完成后才能真测试
4. **关联记忆推迟到 P7**：复杂度远超预期，初期用简单策略替代

---

## 7. API 成本分析

### 7.1 DeepSeek 价格

| 类型 | 价格 |
|------|------|
| 输入 | ~¥1 / 100万 token |
| 输出 | ~¥2 / 100万 token |

### 7.2 各项成本估算

| 场景 | 每次消耗 | 每次成本 | 频率 | 月成本 |
|------|----------|----------|------|--------|
| 普通聊天（20轮） | 3000入 + 500出 | ¥0.004 | 10次/天 | ¥1.20 |
| 记忆提取（LLM） | 1000入 + 300出 | ¥0.0016 | 2次/天 | ¥0.10 |
| 主动消息生成 | 500入 + 150出 | ¥0.0008 | 3次/天 | ¥0.07 |
| 联网搜索增强 | 2000入 + 500出 | ¥0.003 | 1次/天 | ¥0.09 |
| Embedding API | 按量计费 | ~¥0.0001/次 | 10次/天 | ¥0.03 |

**月成本预估：¥1.50 - 5.00**

### 7.3 成本控制措施

1. Token 硬限制：单次请求上下文 ≤ 4000 token
2. LLM 提取节流：每 5 轮对话才触发一次（其余用规则提取）
3. 主动消息日限额：每天最多 10 条
4. 世界书按需检索（不全量注入）
5. 本地成本计数器，APP 内显示预估费用

---

## 8. 风险评估与缓解

### 8.1 风险矩阵

| ID | 风险 | 概率 | 影响 | 等级 | 缓解措施 |
|----|------|------|------|------|----------|
| R1 | ChromaDB 在 Chaquopy 不可用 | 100% | 高 | **已确认** | 改用 DeepSeek Embedding API + 纯 Python 余弦相似度 |
| R2 | 国产 ROM 限制后台任务 | 80% | 高 | **高** | 前台服务保活 + 引导用户加白名单 |
| R3 | DeepSeek API 不稳定 | 30% | 高 | **高** | 3 次重试 + 降级回复 + 离线兜底 |
| R4 | 记忆提取质量不稳定 | 50% | 高 | **高** | 去重校验 + 矛盾检测 + 置信度字段 |
| R5 | Python→Android 性能落差 | 50% | 中 | **中** | P0 阶段做性能基准，限制向量检索 top_k |
| R6 | 主动消息时机不当 | 40% | 中 | **中** | 冷却期 + 用户活跃检测 + 全局开关 |
| R7 | API 成本失控 | 30% | 中 | **中** | Token 硬限制 + 日预算上限 + 本地计数器 |
| R8 | 上下文过长导致延迟 | 60% | 低 | **低** | 动态裁剪 + 摘要压缩 |
| R9 | API Key 泄露 | 15% | 中 | **低** | Android Keystore 加密存储 |

### 8.2 关键边界条件

- **空状态**：首次安装、无记忆、无对话历史
- **极限状态**：对话 500+ 轮、记忆 500 条、单条消息 5000 字
- **异常输入**：纯表情、Unicode 特殊字符、注入攻击
- **状态切换**：WiFi/4G 切换、息屏/亮屏、APP 后台/前台
- **并发**：用户快速发送 + 主动消息同时到达

---

## 9. 交叉审核意见汇总

### 9.1 架构师审核要点

严重问题：
1. ChromaDB 在 Chaquopy 中不可用 → 已确认替代方案（DeepSeek Embedding API）
2. 主动消息在国产 ROM 上的可靠性极低 → 需要前台服务保活
3. 关联记忆复杂度被严重低估 → 推迟到 P7

建议改进：
1. 开发路线调整：P1 加入 Chaquopy 验证，P4 Android 封装提前
2. 遗漏模块：错误处理与降级、对话导出与备份、数据迁移
3. 世界书改为按需检索注入（节省 token）

### 9.2 QA 测试审核要点

高风险：
1. Chaquopy + ChromaDB 兼容性 → 已确认不可用，已确定替代方案
2. DeepSeek API 不稳定 → 需要重试 + 降级 + 离线兜底
3. 记忆提取质量 → 需要去重 + 矛盾检测 + 置信度

UX 风险：
1. 回复延迟 > 3 秒 → 需要流式输出 + "正在输入..."动画
2. 记忆"张冠李戴" → 相似度阈值调优（建议 0.75 起步）
3. 主动消息内容空洞 → 结合最近对话生成个性化问候

### 9.3 后端开发审核要点

可行性确认：所有 Python 模块在 Chaquopy 下**完全可行**。

向量检索方案：推荐 DeepSeek Embedding API + 纯 Python 余弦相似度，5000 条记忆以内检索耗时约 50-100ms。

v1 代码复用：可直接复用 card_parser(95%)、deepseek(90%)、web_search(85%)、base(100%)；需要重构 role_player(40%)、context_manager(70%)、storage(30%)；完全废弃 wx_adapter/、api_server.py、templates/。

---

## 10. 待决策事项

| # | 事项 | 状态 | 建议 |
|---|------|------|------|
| 1 | 确认 Chaquopy 方案 | 待确认 | 三智能体审核通过，方案可行 |
| 2 | 确认向量检索方案 | 待确认 | 推荐 DeepSeek Embedding API + 纯 Python 余弦相似度 |
| 3 | 确认开发路线 | 待确认 | 推荐 P0 原型验证 → P1 Chaquopy 验证 → ... |
| 4 | 第一个角色卡 | 待定 | 用户后续提供 |
| 5 | 语音 TTS | 待定 | 暂不实现，后续根据需求决定 |

---

## 11. 附录

### 11.1 关键术语

| 术语 | 说明 |
|------|------|
| Chaquopy | Android 中嵌入 Python 引擎的开源方案 |
| 角色卡 | JSON 文件，定义 AI 角色的性格、背景等 |
| 世界书 | JSON 文件，定义世界观设定 |
| 强化记忆 | 用户/AI 标记的重要记忆，几乎不衰减 |
| WorkManager | Android 系统后台任务调度器 |
| Embedding | 文本的向量化表示，用于语义检索 |

---

**报告生成时间**：2026-06-04
**审核智能体**：架构师、QA 测试、后端开发
**下一步**：等待用户审核后，进入 P0 Python 原型验证阶段
