# AI 角色扮演聊天 APP — 测试流程报告

> 更新日期：2026-06-07 | 当前阶段：P2 完成，运行时验证待用户执行

---

## 一、测试体系总览

| 阶段 | 测试文件 | 类型 | 状态 |
|------|----------|------|:---:|
| P0 | `test_p0_environment.py` | 环境验证 | ✅ |
| P1-S1 | `test_p1_infrastructure.py` | 基础设施 | ✅ |
| P1-S3 | `test_p1_s3_chat_engine.py` | 聊天引擎 | ✅ |
| P1-S4 | `test_p1_s4_memory.py` | 记忆系统 | ✅ |
| P1-S5 | `test_p1_s5_integration.py` | 集成联调 | ✅ |
| P1-S6 | `test_p1_s6_proactive.py` | 主动消息 | ✅ |
| P2 | `chaquopy_test.py` | Android 运行时 | ⚠️ 待运行时验证 |

---

## 二、P0 环境验证

### 测试文件
`test/test_p0_environment.py`

### 运行命令
```bash
cd f:\Trae AI\ai-APP
python test/test_p0_environment.py
```

### 测试项

| # | 测试项 | 检查内容 | 预期结果 |
|---|--------|----------|----------|
| 1 | Python 版本 | `sys.version_info` | >= 3.10 |
| 2 | 核心依赖 | requests、dotenv、loguru、json5 | 全部可导入 |
| 3 | 目录结构 | src/、data/、test/ 等 12 个目录 | 全部存在 |
| 4 | 配置文件 | .env.example、.gitignore、requirements.txt、pyproject.toml | 全部存在 |

### 合格标准
4 项全部通过，输出 `✅ 所有检查通过！P0 环境配置完成。`

---

## 三、P1-S1 基础设施验证

### 测试文件
`test/test_p1_infrastructure.py`

### 运行命令
```bash
cd f:\Trae AI\ai-APP
python test/test_p1_infrastructure.py
```

### 测试项

| # | 测试项 | 验证内容 |
|---|--------|----------|
| 1 | Settings 配置 | 环境变量读取、默认值、项目根目录 |
| 2 | UserSettings CRUD | get/set/reset、set_many、reset_all |
| 3 | 未知 key 拦截 | 访问不存在的设置项抛出 KeyError |
| 4 | 异常类层次 | APIException 子类、MemoryException 子类 |
| 5 | 时间工具函数 | CST 时区、格式化、时间差计算、跨午夜判断 |
| 6 | retry 装饰器 | 成功重试、重试耗尽、非重试异常直接抛出 |
| 7 | Settings 单例 | 两次实例化返回同一对象 |

### 合格标准
全部 7 项通过，输出 `P1 阶段 1 基础设施验证全部通过！`

---

## 四、P1-S3 聊天引擎验证

### 测试文件
`test/test_p1_s3_chat_engine.py`

### 运行命令
```bash
cd f:\Trae AI\ai-APP
python test/test_p1_s3_chat_engine.py
```

### 测试项

| # | 测试项 | 验证内容 |
|---|--------|----------|
| 1 | 角色卡解析 | JSON 解析、字段完整性、to_prompt_text |
| 2 | 必填字段校验 | 缺少 name/background/personality 抛出异常 |
| 3 | 上下文管理 | 添加消息、获取上下文、token 估算 |
| 4 | 上下文窗口限制 | 超出限制时自动裁剪 |
| 5 | Prompt 构建 | 系统提示词、角色信息、对话历史、示例对话 |
| 6 | 角色扮演流程 | Mock API 调用、完整对话流程 |

### 合格标准
全部通过，角色卡解析正确，上下文管理稳定，Promp 格式正确。

---

## 五、P1-S4 记忆系统验证

### 测试文件
`test/test_p1_s4_memory.py`

### 运行命令
```bash
cd f:\Trae AI\ai-APP
python test/test_p1_s4_memory.py
```

### 测试项

| # | 测试项 | 验证内容 |
|---|--------|----------|
| 1 | 向量存储 CRUD | 添加、查询、更新、删除 |
| 2 | 倒排索引 | 关键词索引、搜索、删除 |
| 3 | 余弦相似度 | 计算精度验证 |
| 4 | 混合检索 | 向量 + 关键词联合检索 |
| 5 | 记忆提取 | 规则模式提取 |
| 6 | 时间衰减 | 衰减系数计算 |
| 7 | 流式导出 | 大数据量导出不 OOM |

### 合格标准
全部通过，检索延迟 < 100ms（5,000 条），提取准确率 > 80%。

---

## 六、P1-S5 集成联调验证

### 测试文件
`test/test_p1_s5_integration.py`

### 运行命令
```bash
cd f:\Trae AI\ai-APP
python test/test_p1_s5_integration.py
```

### 测试项

| # | 测试项 | 验证内容 |
|---|--------|----------|
| 1 | 命令解析 | /help、/card、/world、/memory、/export、/clear、/cost、/quit |
| 2 | 完整对话流程 | Mock API + 多轮对话 |
| 3 | 角色卡切换 | 运行时切换角色卡 |
| 4 | 记忆注入 | 对话中自动注入相关记忆 |
| 5 | 导出功能 | 端到端导出测试 |
| 6 | 错误处理 | API 异常、角色卡缺失、非法输入 |

### 合格标准
全部通过，命令解析正确，对话流程完整，异常处理稳定。

---

## 七、P1-S6 主动消息验证

### 测试文件
`test/test_p1_s6_proactive.py`

### 运行命令
```bash
cd f:\Trae AI\ai-APP
python test/test_p1_s6_proactive.py
```

### 测试项

| # | 测试项 | 验证内容 |
|---|--------|----------|
| 1 | 决策逻辑 | 是否应该发送主动消息的判断 |
| 2 | 消息生成 | 主动消息内容生成 |
| 3 | 话题生成 | 模板 + AI 话题生成 |
| 4 | 调度器持久化 | 状态保存与恢复 |
| 5 | 集成测试 | /proactive 命令 |
| 6 | 边界条件 | 空记忆、无角色卡、异常处理 |

### 合格标准
全部通过，决策逻辑正确，消息生成合理，调度器状态持久化正常。

---

## 八、P2 Chaquopy 运行时验证 ⚠️

### 测试文件
`android/app/src/main/python/chaquopy_test.py`

### 验证方式

#### 方式 A：Android Studio 运行（推荐）
1. 打开 Android Studio
2. `File → Open` → 选择 `F:\Trae AI\ai-APP\android`
3. 连接模拟器或真机
4. 点击 `Run` 按钮
5. 在 Logcat 中搜索 `AICompanion` 查看测试结果

#### 方式 B：命令行安装 APK
```bash
# 安装 APK
adb install android\app\build\outputs\apk\debug\app-debug.apk

# 查看日志
adb logcat -s AICompanion:* System.out:*
```

### 测试项

| # | 测试项 | 验证内容 | 预期结果 |
|---|--------|----------|----------|
| 1 | Python 版本 | `sys.version_info` | `3.10.x` |
| 2 | sqlite3 | 创建表 + 插入 + 查询 | 成功，返回 `(1, 'hello')` |
| 3 | requests | Session 创建 + 请求头设置 | 成功，返回版本号 |
| 4 | 文件 IO | 写入 + 读取 + 删除 | 成功，返回 `Chaquopy test` |
| 5 | Android 路径 | `Environment.getDataDirectory()` | 返回 `/data` 路径 |

### 合格标准
**全部 5 项通过** = P2 Chaquopy 验证完成。

---

## 九、测试执行流程（完整）

```
┌─────────────────────────────────────────────────────────────┐
│                    测试执行顺序                              │
├─────────────────────────────────────────────────────────────┤
│  1. P0 环境验证                                              │
│     python test/test_p0_environment.py                       │
│     ↓ 通过                                                   │
│  2. P1-S1 基础设施验证                                       │
│     python test/test_p1_infrastructure.py                    │
│     ↓ 通过                                                   │
│  3. P1-S3 聊天引擎验证                                       │
│     python test/test_p1_s3_chat_engine.py                    │
│     ↓ 通过                                                   │
│  4. P1-S4 记忆系统验证                                       │
│     python test/test_p1_s4_memory.py                         │
│     ↓ 通过                                                   │
│  5. P1-S5 集成联调验证                                       │
│     python test/test_p1_s5_integration.py                    │
│     ↓ 通过                                                   │
│  6. P1-S6 主动消息验证                                       │
│     python test/test_p1_s6_proactive.py                      │
│     ↓ 通过                                                   │
│  7. P2 Chaquopy 运行时验证                                   │
│     安装 APK → 启动应用 → 查看 Logcat                        │
│     ↓ 通过                                                   │
│  8. P3 核心对话（下一阶段）                                    │
│     聊天 UI + 端到端对话测试                                  │
└─────────────────────────────────────────────────────────────┘
```

---

## 十、快速验证命令

### 一键运行所有 Python 测试（P0-P1）
```bash
cd f:\Trae AI\ai-APP
python test/test_p0_environment.py && `
python test/test_p1_infrastructure.py && `
python test/test_p1_s3_chat_engine.py && `
python test/test_p1_s4_memory.py && `
python test/test_p1_s5_integration.py && `
python test/test_p1_s6_proactive.py
```

### P2 Android 验证
```bash
# 构建 APK
cd android
$env:ANDROID_HOME = "F:\Android\SDK"
$env:JAVA_HOME = "F:\Java\jdk-17"
.\gradlew.bat assembleDebug --no-daemon

# 安装到设备
adb install app\build\outputs\apk\debug\app-debug.apk

# 查看测试日志
adb logcat -s AICompanion:* System.out:*
```

---

## 十一、测试覆盖统计

| 类别 | 测试文件数 | 测试项数 | 通过率 |
|------|:---:|:---:|:---:|
| 环境验证 | 1 | 4 | 100% |
| 基础设施 | 1 | 7 | 100% |
| 聊天引擎 | 1 | 6 | 100% |
| 记忆系统 | 1 | 7 | 100% |
| 集成联调 | 1 | 6 | 100% |
| 主动消息 | 1 | 6 | 100% |
| Android 运行时 | 1 | 5 | ⚠️ 待验证 |
| **总计** | **7** | **41** | **36/36 (Python) + 5 待验证** |

---

*报告生成时间：2026-06-07 19:10*