# V5.2 四智能体交叉审查报告

**审查日期**：2026-06-10  
**审查方式**：4 个智能体并行交叉审查  
**审查范围**：安全性、代码质量、架构设计、QA/Bug  

---

## 一、本次已修复的问题（本轮会话）

| 编号 | 类别 | 描述 | 文件 |
|:----:|:-----|:-----|:-----|
| F11 | 正则 | "我"改为可选(`我?`)，支持中文流水句省略主语 | `extractor.py` |
| F12 | 代码质量 | 空 except 块增加日志记录 | `chat_bridge.py` L71, L244 |
| F13 | 代码质量 | `init_memory` 返回值统一为 dict（移除 json.dumps） | `chat_bridge.py` L261 |
| F14 | 封装 | 新增 `DeepSeekClient.set_model()` 公开方法 | `deepseek.py` L114-121 |
| F15 | 封装 | `role_player.py` 改用 `client.set_model()` 替代私有属性 | `role_player.py` L105, L109 |
| F16 | Bug | `_extract_by_llm` 中 `.format()` → `.replace()` 避免花括号崩溃 | `extractor.py` L273 |
| F17 | Bug | `retriever.retrieve()` 增加 embed 空列表检查 | `retriever.py` L45-48 |
| F18 | Bug | `retriever.retrieve_by_type()` 增加 embed 空列表检查 | `retriever.py` L84-87 |

---

## 二、安全性发现（待修复）

| # | 风险 | 严重程度 | 建议 |
|---|------|:--------:|------|
| S1 | API Key 明文 SharedPreferences 存储 | **HIGH** | 改用 `EncryptedSharedPreferences` |
| S2 | 记忆数据明文 SQLite 存储（姓名/地址/手机号等） | **HIGH** | AES 加密 content 字段 |
| S3 | 日志泄露用户对话内容（INFO 级别） | MEDIUM | Release 构建设为 WARNING 级别 |
| S4 | 导出功能无加密选项 | MEDIUM | 提供加密导出 |
| S5 | 异常消息直接返回前端，可能泄露内部信息 | MEDIUM | 区分面向用户/开发者的错误消息 |
| S6 | 无 SSL Certificate Pinning | MEDIUM | Release 构建添加证书固定 |

**隐私安全总结**：
- `allowBackup="false"` ✅ 正确
- `usesCleartextTraffic="false"` ✅ 正确
- 权限声明精简（仅 INTERNET + ACCESS_NETWORK_STATE）✅ 正确
- 无硬编码密钥 ✅ 正确
- SQL 全参数化查询 ✅ 正确
- 无 eval/exec/pickle 风险 ✅ 正确

---

## 三、代码质量发现（待修复）

| # | 严重程度 | 描述 | 位置 |
|---|:--------:|------|------|
| Q1 | HIGH | 8 个函数超过 30 行限制（最长 90 行） | role_player, orchestrator, extractor 等 |
| Q2 | HIGH | `_auto_remember` 缺少防御性 None 检查 | chat_bridge.py L197 |
| Q3 | MEDIUM | `vector_store.search()` N+1 数据库写入 | vector_store.py L334 |
| Q4 | MEDIUM | `retriever.py` 硬编码权重系数 0.6/0.4 | retriever.py L63 |
| Q5 | MEDIUM | `MainActivity.kt` 脆弱正则 JSON 降级解析 | MainActivity.kt L374 |
| Q6 | MEDIUM | `orchestrator.remember()` 三层 try/except 嵌套 | orchestrator.py L125-150 |

---

## 四、Bug 发现（待修复）

| # | 严重程度 | 描述 | 位置 |
|---|:--------:|------|------|
| B1 | HIGH | `DeepSeekClient` 初始化后 `set_api_key()` 不更新 session header | deepseek.py |
| B2 | MEDIUM | `_extract_by_llm` JSON 解析失败静默丢弃所有提取记忆 | extractor.py L291 |
| B3 | MEDIUM | `init_memory` 重置 `_turn_counter` 导致 source_turn_id 可能重复 | chat_bridge.py L258 |
| B4 | MEDIUM | 极短文本（3字符）bigram Jaccard 去重准确性未验证 | extractor.py |
| B5 | LOW | `resetChat()` 无防重复点击保护 | MainActivity.kt L323 |
| B6 | LOW | `vector_store.search()` 全0分排序键为空时不稳定 | vector_store.py L331 |

---

## 五、架构建议

| # | 优先级 | 建议 | 理由 |
|---|:------:|------|------|
| A1 | P0 | MainActivity 拆分（507 行 → 提取 SettingsManager） | 当前职责过重 |
| A2 | P1 | DeepSeekClient 抽象为 Protocol 接口 | 方便未来接入 OpenAI/Claude |
| A3 | P1 | 接入替代 embedding 服务 | 当前 DeepSeek 无 /v1/embeddings，语义检索已失效 |
| A4 | P2 | 记忆检索增加缓存层 | 同 query 短时间内重复检索 |
| A5 | P2 | 参考 SillyTavern 世界书系统完善 lorebook | 角色背景知识管理 |

---

## 六、正面评价

- Custom 异常体系设计合理（`APIKeyError`, `APIQuotaError`, `APIRateLimitError` 等）
- 向量存储线程安全（`threading.Lock`）、WAL checkpoint 机制、TOCTOU 保护
- `role_player.chat()` finally 清空本轮记忆——防止幽灵记忆
- Settings 单例模式 + dotenv 优雅降级
- 指数退避重试装饰器实现规范
- 降级机制完善（embedding→关键词→兜底）

---

## 七、下一步行动建议

| 优先级 | 行动 | 预计工时 |
|:------:|------|:--------:|
| **立即** | 继续 P0 手机验证（P0-4 ~ P0-8） | 1h |
| **本迭代** | S1 API Key 加密存储 | 2h |
| **本迭代** | B1 `set_api_key` session header 更新 | 0.5h |
| **本迭代** | S3 日志级别调整（Release WARNING） | 0.5h |
| **下迭代** | S2 记忆数据加密 | 4h |
| **下迭代** | A2 LLM 后端 Protocol 抽象 | 3h |
| **技术债务** | Q1 超长函数拆分 | 分批 |
| **技术债务** | A3 替代 embedding 服务 | 1d |