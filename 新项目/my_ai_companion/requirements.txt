# 核心依赖（Chaquopy 兼容，纯 Python 包）
requests>=2.32.0
python-dotenv>=1.0.0
loguru>=0.7.0
# chaquopy 是 Android Gradle 插件，在 build.gradle 中配置，不是 pip 包
json5>=0.9.0

# 可选依赖（按需引入）
# numpy>=1.26.2          # K 级记忆向量加速（+4.8MB）
# scikit-learn>=1.3.2    # 高级相似度计算（+8.4MB）
# cryptography>=41.0.0   # 加密导出（+3MB）